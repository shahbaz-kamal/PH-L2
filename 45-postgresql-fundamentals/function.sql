-- scaler funcytions (upper,lower,concat,length)
-- aggregate functions
select
    length(chestpaintype) as concating
from
    heart


    -- aggregate

    select * from heart

    select avg(age) from heart

    select count(*) from students